import 'package:flutter/material.dart';

class MissionAvailableListTile extends StatelessWidget {
  const MissionAvailableListTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
